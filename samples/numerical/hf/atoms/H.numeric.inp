# Molecule definition
molecule {
H  0.000   0.000   0.000
}

# basis-set specification
basis {
    e- STO-3G
}

# scf options
scf {
    hf
    numeric
}

grid {
    e- [100, 14]; ablmax: 0; abldep: 1.0e-6
}

code {
result = lambda x: print("PASS!") if abs(x) < 1e-5  else print("FAILED!")
ref_energy = -0.5
diff_energy = ref_energy - Energy
result(diff_energy)    
}
