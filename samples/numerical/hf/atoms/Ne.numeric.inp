# Molecule definition
molecule [e-:0:1]{
    Ne  0.000   0.000   0.000
}

# basis-set specification
basis {
    e- STO-3G
}

# scf options
scf {
    hf
    numeric
    aux_basis
    aux_basis_ldep = 1.0e-6
    aux_basis_lmax = 2
}

grid {
   e- [200, 26]
}

code {
result = lambda x: print("PASS!") if abs(x) < 1e-4  else print("FAILED!")
ref_energy = -128.547098
diff_energy = ref_energy - Energy
result(diff_energy)    
}
