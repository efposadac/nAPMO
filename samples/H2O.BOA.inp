# Molecule definition
molecule {
    O  0.000   0.000   0.113
    H  0.000   0.752   -0.451
    H  0.000   -0.752  -0.451
}

# basis-set specification
basis {
    e- STO-3G
}

# scf options
scf {
    # direct # for eris calculation on-the-fly (slower)
    hf
    numeric
    grid = [100, 110]
}

code {
result = lambda x: print("PASS!") if abs(x) < 1e-7  else print("FAILED!")
ref_energy = -76.032776981798
diff_energy = ref_energy - Energy
result(diff_energy)    
}

